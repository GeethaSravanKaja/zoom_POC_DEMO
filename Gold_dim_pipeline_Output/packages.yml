# =====================================================
# DBT Packages Configuration
# Description: External packages required for the Gold Dimension Pipeline
# Author: Data Engineer
# =====================================================

packages:
  # dbt-utils: Collection of useful macros and tests
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
    
  # dbt-audit-helper: Helps with data validation and auditing
  - package: dbt-labs/audit_helper
    version: [">=0.9.0", "<1.0.0"]
    
  # dbt-expectations: Additional data quality tests
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]

# Alternative package sources (if needed)
# - git: "https://github.com/dbt-labs/dbt-utils.git"
#   revision: v1.1.1

# Local packages (if any custom packages are developed)
# - local: ../path/to/local/package